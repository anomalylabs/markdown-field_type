# Configuration

**Example Definition:**

```
protected $fields = [
    'example' => [
        'type'   => 'anomaly.field_type.markdown',
        'config' => [
            'height' => 500
        ]
    ]
];
```

### `height`

Height of the markdown editor. The default value is `300`.

